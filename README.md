# Coinsplit

## Run the Expo app

```bash
npm install
npm run start
```

You can also launch a simulator directly with:

```bash
npm run ios
npm run android
```

The initial app boots into the `Ledger List` screen with an empty state.
